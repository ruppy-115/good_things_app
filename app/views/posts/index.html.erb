<div class="pt-3">
  <div class="w-10/12 mx-auto">
    <!-- 掲示板一覧 -->
    <% if @posts.present? %>
      <div class="flex flex-col">
        <%= render @posts %>
      </div>
    <% else %>
      <div class="mb-3">掲示板がありません</div>
    <% end %>
  </div>
</div>
