<div class="pt-3 mt-16">
  <div class="w-10/12 mx-auto">
    <h1 class="text-3xl font-bold text-center">みんなの記録一覧</h1>
    <!-- 掲示板一覧 -->
    <% if @posts.present? %>
      <div class="flex flex-col mt-12">
        <%= render @posts %>
      </div>
    <% else %>
      <div class="mb-3">掲示板がありません</div>
    <% end %>
  </div>
</div>
